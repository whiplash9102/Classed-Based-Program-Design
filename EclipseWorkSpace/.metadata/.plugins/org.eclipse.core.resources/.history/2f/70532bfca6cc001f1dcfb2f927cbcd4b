package lab3;

public interface IShape {
	//Compute area of the shape
	double area();
	
	//To compute the distance from this shape to the origin
	double distanceToOrigin();
	
	//To increase the size of this shape by given increment
	IShape grow(int inc);
	
	//Is the area of this shape bigger than the area of that shape
	boolean isBiggerThan(IShape that);
	
}

