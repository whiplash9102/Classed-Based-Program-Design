package lab5;

public interface ILoBook {
	
	// count the book in this list
	int count();
	
	//produce a list of all books published before the given date
	// from this list of books
	ILoBook allBefore(int year);
	
	//calculate the total sale price of all books in list
	double salePrice(int discount);
	
	//produce a list of all book in this list, sorted by price
	ILoBook sortByPrice();

	ILoBook insertByPrice(Book book);
	
	ILoBook sortByTitle();

	ILoBook insertByTitle(Book book);
}
