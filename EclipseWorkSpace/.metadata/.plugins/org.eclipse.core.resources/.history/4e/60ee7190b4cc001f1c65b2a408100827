package lab3;
import tester.*;

public class ExamplesShape {
  // Instance of the shape to test
  IShape c1 = new Circle(50, 50, 10, "red");

  // Test method for area
  boolean testIShapeArea(Tester t) {
      return t.checkInexact(c1.area(), 3115, 0.0);
  }

}